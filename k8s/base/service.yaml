apiVersion: v1
kind: Service
metadata:
  name: moreai-service
  namespace: moreai
spec:
  selector:
    app: moreai-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP